# Railway 배포를 위한 환경 변수 예제
# .env 파일로 복사하고 실제 값으로 변경하세요

# Database (Railway PostgreSQL)
# Railway는 자동으로 DATABASE_URL을 생성합니다.
# PostgreSQL 플러그인이 설치되어 있어야 합니다.
# Railway에서 데이터베이스 서비스를 생성한 후 확인하세요: https://railway.app/new
# 또는 Railway가 제공하는 DATABASE_URL 환경 변수를 사용할 수 있습니다.
DATABASE_URL=

# Supabase (Alternative to local PostgreSQL)
# Supabase를 사용할 경우 DATABASE_URL을 Supabase 제공하는 URL로 덮어씌우세요.
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_DB_URL=

# Security
SECRET_KEY=your-production-secret-key-change-this
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# SMTP (선택 사항 - 이메일 알림 기능 시 필요)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Application
DEBUG=False
# 프론트엔드 URL (쿠바 구분)
ALLOWED_ORIGINS=https://easyk.vercel.app

# Payment
TOSS_CLIENT_KEY=
TOSS_SECRET_KEY=
